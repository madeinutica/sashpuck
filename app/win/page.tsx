import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Win",
};

export default function WinPage() {
  return (
    <div>
      <h1>Win</h1>
      <p>This is the win page.</p>
    </div>
  );
}
